function CodelandUsernameValidation(str) { 

  let regex = /^[a-zA-Z][a-zA-Z0-9_]*[^_]$/
  
  return str.length <= 25 && str.length >= 4 && regex.test(str)
}
   
console.log(CodelandUsernameValidation(readline()));

